name: Release Docker

on:
  push:
    tags:
      - '*'

permissions:
  contents: read

jobs:
  push-release-to-registry:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@__VERSION__

      - name: Set up QEMU
        uses: docker/setup-qemu-action@__VERSION__

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@__VERSION__

      - name: Log in to Docker Hub
        uses: docker/login-action@__VERSION__
        with:
          username: ${{ secrets.DOCKER_USER }}
          password: ${{ secrets.DOCKER_PASS }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@__VERSION__
        with:
          images: yodashut/force-crystal

      - name: Build and push Docker image
        uses: docker/build-push-action@__VERSION__
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          platforms: linux/amd64,linux/arm64
