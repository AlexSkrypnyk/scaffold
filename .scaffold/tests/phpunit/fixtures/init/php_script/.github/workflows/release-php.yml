@@ -39,27 +39,10 @@
       - name: Install dependencies
         run: composer install
 
-      - name: Set release version
-        env:
-          INPUTS_VERSION: ${{ github.event.inputs.version }}
-          REF_NAME: ${{ github.ref_name }}
-        run: |
-          version="${INPUTS_VERSION:-${REF_NAME}}"
-          echo "RELEASE_VERSION=$version" >> "$GITHUB_ENV"
-          echo "Release version: $version"
-          sed -i "s/\"force-crystal-version\": \"development\"/\"force-crystal-version\": \"${version}\"/g" box.json
 
-      - name: Build PHAR
-        run: composer build
-
-      - name: Test PHAR
-        run: |
-          ./.build/force-crystal.phar --version
-          ./.build/force-crystal.phar || exit 1
-
       - name: Create Release
         uses: softprops/action-gh-release@__HASH__ # __VERSION__
         with:
           tag_name: ${{ env.RELEASE_VERSION }}
           files: |
-            ./.build/force-crystal.phar
+            force-crystal
