@@ -50,7 +50,7 @@
 
       - name: Set PHAR version
         run: |
-          sed -i "s/\"force-crystal-version\": \"development\"/\"force-crystal-version\": \"${RELEASE_VERSION}\"/g" box.json
+          sed -i "s/\"star-forge-version\": \"development\"/\"star-forge-version\": \"${RELEASE_VERSION}\"/g" box.json
 
       - name: Build PHAR
         run: composer build
@@ -57,8 +57,8 @@
 
       - name: Test PHAR
         run: |
-          ./.build/force-crystal.phar --version
-          ./.build/force-crystal.phar || exit 1
+          ./.build/star-forge.phar --version
+          ./.build/star-forge.phar || exit 1
 
       - name: Create Release
         uses: softprops/action-gh-release@__HASH__ # __VERSION__
@@ -65,4 +65,4 @@
         with:
           tag_name: ${{ env.RELEASE_VERSION }}
           files: |
-            ./.build/force-crystal.phar
+            ./.build/star-forge.phar
